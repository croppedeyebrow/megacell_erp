# infra

ERP 운영과 배포에 필요한 인프라성 파일을 보관합니다.

## 하위 폴더

- `scripts/`: 서버 실행, 종료, 반자동 배포 배치 파일
- `cloudflare/`: Cloudflare Tunnel, Access, 도메인 설정 메모

ERP 화면 코드와 업무 로직은 `views/`, `services/`, `core/`에서 관리합니다.
